---
layout: layouts/base.njk
---
<div class="w-full bg-orange-100 py-8">
    <div class="px-4 flex">
        <div class="flex-col m-4 bg-white rounded-lg h-28">
            <img class="w-8 mx-3" src={{charimg}}>
            <p class="text-xs p-3" >{{charname}}</p>
        </div>
        <div class="w-2/3">
            <div class="speech-box bg-white rounded-lg p-6 mt-4">
                <h1 class="text-xl italic text-bold pb-4">{{  greeting  }}</h1>
                <p class="max-w-prose">{{ content | safe }}</p>
            </div>
            <div class="flex flex-col bg-white rounded-lg mt-8 p-4 bg-opacity-50">
                <p class="italic">quest finder</p>
                <div class="flex flew-row">
                    {% for q in images %}
                            <div class="px-6">
                                <img class="w-8" src={{q.img}}>	
                                {# <p class="w-64 text-sm" >{{thumb.caption}}</p> #}
                            </div>
                    {% endfor %}	
                <div>
            </div>
        </div>
    </div>


    
</div>

</div>