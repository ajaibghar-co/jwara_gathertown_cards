---
layout: layouts/base.njk
---
<div class="w-full">
    <div class="mt-10 flex place-content-center">
        <div class="flex-col m-4">
            <img class="w-20 mx-3" src={{charimg}}>
            <p class="text-xs text-center p-3" >{{charname}}</p>
        </div>
        <div class="w-2/3">
            <div class="speech-box backdrop-blur-sm bg-white/30 rounded-lg p-6 mt-4">
                <h1 class="text-xl text-white italic text-bold pb-4">{{  greeting  }}</h1>
                <p class="max-w-prose text-white ">{{ content | safe }}</p>
            </div>
            <div class="flex flex-col backdrop-blur-sm bg-white/30 rounded-lg mt-8 p-4 bg-opacity-50">
                <p class="italic">quest finder</p>
                <div class="flex flew-row">
                    {% for q in images %}
                            <div class="px-6">
                                <img class="w-8" src={{q.img}}>	
                                {# <p class="w-64 text-sm" >{{thumb.caption}}</p> #}
                            </div>
                    {% endfor %}	
                <div>
            </div>
        </div>
    </div>


    
</div>

</div>